services:
  db:
    image: postgres:13
    container_name: florestou-db
    restart: always
    environment:
      POSTGRES_DB: florestou
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - ./.postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"